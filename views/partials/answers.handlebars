<div class="answers">
    {{#each this}}
    <section class="answer">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{answer_text}}
        </div>
    </section>
    {{/each}}
</div>


