<ol class="question-list">
    {{#each questions as |question|}}
    
        {{> question-info question }}
    
    {{/each}}
</ol>
{{!-- <ol class="question-list">
    <div class="questions">
        {{#each questions}}
        <article class="card">
            <div class="card-header flex-row justify-space-between">
                <h2>
                    <a href="/question/{{id}}">{{title}}</a>
                </h2>
                <div>
                    questioned by {{User.username}} on {{format_date createdAt}}
                </div>
            </div>
            <div class="card-body">
                {{body}}
            </div>
        </article>
        {{/each}}
    </div>
</ol> --}}

{{!-- <div class="questions">
  {{#each questions}}
  <article class="card">
    <div class="card-header flex-row justify-space-between">
      <h2>
        <a href="/question/{{id}}">{{title}}</a>
      </h2>
      <div>
        questioned by {{User.username}} on {{format_date createdAt}}
      </div>
    </div>
    <div class="card-body">
      {{body}}
    </div>
  </article>
  {{/each}}
</div> --}}